{
  "name": "brain-soldiers",
  "private": true,
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "build": "npm --prefix backend install && npm --prefix frontend install && npm --prefix frontend run build && node ./scripts/copy-frontend-dist.mjs",
    "start": "npm --prefix backend start"
  }
}
